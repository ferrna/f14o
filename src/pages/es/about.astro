---
import Base from '../../layouts/Base.astro';
import About from '../../components/About.astro';
import { useCopy } from '../../i18n/ui';

const lang = 'es';
const c = useCopy(lang);
---

<Base title={c.meta.aboutTitle} description={c.meta.aboutDescription} lang={lang} routeLabel={c.route.about}>
  <About lang={lang} />
</Base>
